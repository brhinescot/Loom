namespace Loom.Web.Portal.Controllers
{
    // TODO: Create a TemplateController that just loads the templates configured in the database.
    public interface IController
    {
        dynamic ViewData { get; }
    }
}