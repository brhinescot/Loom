namespace Loom.Web.UI.PageParts
{
    public enum ClientEvent
    {
        OnClick,
        OnMouseOver,
        OnMouseOut,
        OnBlur,
        OnFocus,
        OnAbort,
        OnError,
        OnLoad,
        OnChange,
        OnSelect
    }
}